[build]
builder = "dockerfile"
dockerfilePath = "./Dockerfile"
watchPatterns = ["src/**", "public/**", "package.json", "Dockerfile"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

